<div dir="rtl">

# 📤 הוראות העלאה ל-GitHub + הפעלת GitHub Pages

מסמך זה מסביר **מה נבנה** ו**איך להעלות** את תת-הפרויקט הזה ל-GitHub ולקבל קישור חי לפורטפוליו/לינקדאין.
כתוב שלב-אחר-שלב, כולל הפקודות המדויקות.

---

## חלק א' — מה נבנה כאן (סיכום)

יצרתי בתוך `to_github/` **פרויקט הדגמה סטטי ועצמאי** של מפת החום, בלי לגעת בפרויקט הראשי:

1. **המרה לאתר סטטי (ללא שרת).** מפת החום היא קריאה-בלבד, ולכן ויתרתי על Flask: את הטבלאות מ-SQLite
   "הקפאתי" לקובצי JSON תחת `data/`. הקוד בצד הלקוח (`app.js`) זהה למקור — שיניתי **רק** את פונקציית
   `api()` כך שתתרגם כל נתיב `/api/*` לקובץ `data/*.json`. שאר הקוד לא נגע.
2. **נתונים סינתטיים בלבד.** כתבתי מחולל ([`tools/generate_demo_data.py`](tools/generate_demo_data.py))
   שמגריל את **כל** המספרים (אחוזי איוש, תקן/מאויש, דמוגרפיה, זמני נסיעה, מועמדים) עם seed קבוע.
   נשמרו רק **שמות התחנות והיישובים והקואורדינטות** — גאוגרפיה ציבורית. אף נתון אמת לא נכנס לכאן.
3. **README מקצועי** עם תמונת תצוגה מקדימה, הבעיה העסקית, הארכיטקטורה וההחלטות הטכניות.
4. **`.gitignore`** שחוסם `*.db`, `*.xlsx`, `*.csv`, `data/real/` ועוד — שכבת הגנה שלא ייכנס נתון אמת.
5. **הבהרת נתונים סינתטיים** מופיעה גם ב-README וגם כבאנר בתוך האפליקציה עצמה.

מה שלא עובד בגרסה הסטטית (במכוון): טעינת אקסל, עריכת נתונים, וייצוא לאקסל — אלה דורשים שרת.
הם מסומנים כמושבתים בממשק. הצפייה בכל המסכים, המפה והדשבורדים — מלאה.

---

## חלק ב' — למה מאגר (repo) חדש ולא הקיים ⚠️

**קריטי:** אל תעלה את הריפו הקיים של `geo-heat`. גם אם תמחק ממנו קבצים, **קובצי אקסל אמיתיים
ו-`heatmap.db` נשארים ב-git history** של אותו ריפו לנצח, וניתן לשחזר אותם. הדרך הבטוחה היחידה
היא **מאגר חדש לגמרי**, שההיסטוריה שלו מתחילה מהקבצים הסינתטיים כאן בלבד.

לכן ההוראות למטה יוצרות היסטוריית git **טרייה** מתוך תיקיית `to_github` בלבד.

---

## חלק ג' — העלאה שלב-אחר-שלב

### שלב 1: העתקת התיקייה למקום נקי
העתק את **תוכן** `to_github` לתיקייה חדשה מחוץ לפרויקט הראשי (כדי לא לסחוב את היסטוריית geo-heat):

<div dir="ltr">

```bash
# החלף בנתיב שנוח לך:
cp -r "/mnt/c/projects/geo-heat/to_github" ~/police-recruitment-heatmap
cd ~/police-recruitment-heatmap

# מחיקת מסמך ההנחיות (לא צריך לעלות לפורטפוליו) — ה-.gitignore ממילא חוסם *.docx
rm -f *.docx "~$"*.docx
```

</div>

### שלב 2: אתחול git טרי
<div dir="ltr">

```bash
git init
git add .
git status                     # ודא שאין *.db / *.xlsx ברשימה — רק קוד ו-data/*.json
git commit -m "מפת חום גיוס — הדגמת פורטפוליו (נתונים סינתטיים)"
```

</div>

### שלב 3: יצירת מאגר ציבורי חדש ב-GitHub
1. היכנס ל-**github.com** ← לחצן **New** (יצירת repository).
2. **Repository name:** לדוגמה `police-recruitment-heatmap`.
3. **Public** (חובה — GitHub Pages בחינם דורש ריפו ציבורי).
4. **אל תסמן** "Add a README" (כבר יש לנו). ← **Create repository**.

### שלב 4: חיבור ודחיפה
העתק את הפקודות ש-GitHub מציג ("…or push an existing repository"), או השתמש באלה
(החלף `<username>` ו-`<repo-name>`):

<div dir="ltr">

```bash
git branch -M main
git remote add origin https://github.com/<username>/<repo-name>.git
git push -u origin main
```

</div>

### שלב 5: הפעלת GitHub Pages
1. במאגר ב-GitHub ← **Settings** ← בתפריט הצד **Pages**.
2. תחת **Source** בחר **Deploy from a branch**.
3. **Branch:** `main` · **Folder:** `/ (root)` ← **Save**.
4. המתן דקה-שתיים. בראש עמוד ה-Pages יופיע הקישור:
   **`https://<username>.github.io/<repo-name>/`**

### שלב 6: עדכון הקישור ב-README
ערוך את `README.md`, החלף את השורה `🔗 הדגמה חיה` בקישור האמיתי, ודחוף:

<div dir="ltr">

```bash
git add README.md
git commit -m "עדכון קישור ההדגמה החיה"
git push
```

</div>

---

## חלק ד' — בדיקה אחרי העלייה ✅

- [ ] הקישור `https://<username>.github.io/<repo-name>/` נפתח ומראה את המפה תוך שנייה.
- [ ] הסיכות צבועות (אדום/כתום/צהוב/ירוק) — סימן שה-JSON נטען.
- [ ] הבאנר "מצב הדגמה" מופיע בסרגל הצד.
- [ ] לוח הבקרה, ממשק המגייס והתכנון האסטרטגי מציגים מספרים.
- [ ] אין ברשימת הקבצים במאגר אף `*.db` או `*.xlsx`.

אם המפה ריקה: כמעט תמיד זו בעיית **אותיות רישיות בנתיב** — ודא ש-`data/` והקבצים בתוכו נדחפו,
ושהשמות תואמים בדיוק (GitHub Pages רגיש לאותיות, שלא כמו Windows).

---

## הערה: "תיקייה של כלל הפרויקטים"

זה **פרויקט אחד** מתוך הפורטפוליו. שתי גישות:

- **מומלץ — ריפו לכל פרויקט:** כל פרויקט הוא repo נפרד עם קישור Pages נקי משלו
  (`username.github.io/police-recruitment-heatmap`). הכי קריא למעסיק ופשוט לתחזוקה. זו הגישה שההוראות
  למעלה מיישמות.
- **חלופה — ריפו פורטפוליו אחד:** אם תעדיף איגוד תחת מאגר אחד, שים כל פרויקט בתת-תיקייה
  (למשל `police-recruitment-heatmap/`, ובעתיד `project-2/`), הפעל Pages על ה-root, והמפה תהיה
  ב-`username.github.io/portfolio/police-recruitment-heatmap/`. במקרה כזה כדאי גם `index.html`
  ראשי שמקשר לכל הפרויקטים.

בכל דרך שתבחר — הקבצים כאן מוכנים כמו שהם.

</div>
